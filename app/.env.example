# Supabase client config — read by Expo CLI at bundle time (EXPO_PUBLIC_* vars
# are inlined into the JS bundle). Copy to .env for local development.
#
# These are the PUBLISHABLE values for the shared momentum_engine project
# (uofnnmixmjqhoumbbcfe). The publishable/anon key is safe to expose — every
# table access goes through RLS — but env is preferred over hard-coding.
# NEVER put a service-role key anywhere in this app.
EXPO_PUBLIC_SUPABASE_URL=https://uofnnmixmjqhoumbbcfe.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_5wCY7E7R7cnqFaeOv9MWMg_Hywh0NuJ
